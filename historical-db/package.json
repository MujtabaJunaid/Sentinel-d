{
  "name": "sentinel-d-historical-db",
  "version": "1.0.0",
  "description": "Historical DB write client (Cosmos DB) and deferred backlog writer (Table Storage)",
  "main": "write-client.js",
  "scripts": {
    "prebuild": "node -e \"require('fs').cpSync('../../shared/schemas/historical_db_record.json','schemas/historical_db_record.json')\"",
    "build": "npm run prebuild",
    "test": "jest --verbose",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/data-tables": "^13.2.0",
    "@azure/identity": "^4.0.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
